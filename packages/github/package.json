{
  "name": "@jsx2md/github",
  "version": "0.0.1",
  "description": "GitHub Markdown components and adapters for jsx2md.",
  "keywords": [
    "gfm",
    "github",
    "jsx",
    "markdown"
  ],
  "license": "MIT",
  "files": [
    "dist",
    "README.md"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "rm -rf dist && tsup src/index.ts --format esm --dts --sourcemap --clean --tsconfig tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "jsx2md": "workspace:*"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
